@model IEnumerable<theNoteTaker_v1.Models.Note>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Note1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CreatedOn)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.Raw(item.Note1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CreatedOn)
        </td>
        <td>
                
                @Html.ActionLink(" ", "Edit", new { id = item.NoteID},new { @class = "glyphicon glyphicon-pencil" })
                @Html.ActionLink(" ", "Details", new { id = item.NoteID }, new { @class = "glyphicon glyphicon-eye-open" }) 
                @Html.ActionLink(" ", "Delete", new { id = item.NoteID }, new {@class="glyphicon glyphicon-remove-circle"})


</td>
            
        
    </tr>
}

</table>
